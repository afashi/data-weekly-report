# 需求规格 vs 代码实现 - 差距对比分析报告

> **生成时间**: 2026-01-24
> **任务类型**: 全栈 (→ 并行)
> **Codex Session**: `019beba8-c5a1-7750-bb40-62782301e0fc`
> **Gemini Session**: `9b0e273f-0624-4853-b507-816afbd6b1c8`

---

## 📊 执行摘要

本报告基于多模型协作分析（Codex 后端分析 + Gemini 前端分析），对比了《数据周报自动化系统 - 需求规格说明书 V3.0》与当前代码实现，识别出
**13 个关键差距** 和 **7 个高优先级修复项**。

**⚠️ 用户需求调整**：

- ✅ 不需要 Excel Sheet 4 维度说明（已从计划中移除）
- ✅ 新增：UI/样式全面优化需求（现有样式需要美化）
- ✅ 新增：页面布局重构需求（版本选择器和生成按钮应独立，新增按钮放在表格底部）

**总体完成度评估**：

- ✅ **数据模型对齐度**: 95% （4 张表结构完全符合，BIGINT ID 策略正确）
- ⚠️ **API 接口对齐度**: 70% （核心 CRUD 已实现，但路径命名与规格不一致）
- ❌ **前端 UI 对齐度**: 40% （布局结构需重构，样式需全面优化）
- ⚠️ **Excel 导出对齐度**: 60% （功能实现但文件名与样式有偏差）

---

## 🔍 详细差距分析

### 1. API 路径命名不一致（高优先级）

#### 问题描述

**需求规格说明书** (Section 5.1 核心 API 资源)：

```
POST   /api/reports/generate         - 触发 ETL 流程生成新周报
GET    /api/reports/:id/export       - 导出 Excel 文件流
PUT    /api/reports/:id/manual-items - 全量更新自采数据 Tab
```

**实际实现**：

```
POST   /api/generate                      - ✅ 已实现但路径不符
GET    /api/export/:reportId              - ✅ 已实现但路径不符
PUT    /api/items/reports/:id/manual-items - ✅ 已实现但路径不符
```

**影响范围**：

- 🔴 **API 契约漂移**：前后端已适配错误路径，形成技术债务
- 🔴 **文档不一致**：规格说明书无法作为前端 API 调用参考

**关键文件**：

- `backend/src/modules/generate/generate.controller.ts:9` - 需修改路由前缀
- `backend/src/modules/export/export.controller.ts:16` - 需修改路由路径
- `backend/src/modules/items/items.controller.ts:81` - 需修改路由路径
- `frontend/src/services/generate-api.ts:9` - 需同步修改调用路径
- `frontend/src/services/export-api.ts:14` - 需同步修改调用路径
- `frontend/src/services/item-api.ts:42` - 需同步修改调用路径

**技术方案**：

- **方案 A（推荐）**：添加兼容层 - 在 ReportsController 中添加路由别名，保持向后兼容
- **方案 B**：大改 - 修改所有后端路由并同步前端，破坏性变更
- **方案 C**：修规格 - 更新需求规格说明书以匹配现有实现，技术债务化

---

### 2. SQL 查询适配器功能缺陷（高优先级）

#### 问题描述

**需求规格说明书** (Section 1.2 核心业务流程)：
> 2. **快照生成**：...并发拉取 Jira 任务与**数据库指标**，计算本周时间周期...

**实际实现问题**：

1. **参数绑定缺失**：`app.yaml` 中的 SQL 查询包含 `$1` 参数占位符，但 `SqlAdapter.executeQuery()` 未传递参数
2. **数据库路由错误**：配置支持多数据库，但适配器始终使用默认库
3. **字段映射错误**：`statusCode` 从 `row.status` 映射，但查询结果可能无此字段

**关键文件**：

- `backend/config/app.yaml:50-58` - SQL 查询配置
- `backend/src/modules/generate/adapters/sql.adapter.ts:38` - `fetchBrvMetrics` 方法
- `backend/src/modules/generate/adapters/sql.adapter.ts:145` - `executeQuery` 方法
- `backend/src/modules/generate/adapters/sql.adapter.ts:175` - `normalizeMetrics` 方法

**影响**：

- 🔴 **指标数据不准确**：ETL 状态可能显示错误或空值
- 🟡 **多数据库支持失效**：无法从不同环境的 PostgreSQL 读取数据

**修复优先级**：⭐⭐⭐ 高（影响业务数据准确性）

---

### 3. Excel 导出格式与规格不符（高优先级）

#### 问题描述

**用户需求**（已调整）：

```
1. Sheet 1 - 概览（保留）
2. Sheet 2 - 本周完成
3. Sheet 3 - 自采数据（主任务：整行背景色加深，字体加粗）
4. Sheet 4 - 后续计划
5. Sheet 5 - 会议待办（保留）
❌ 不需要：维度说明 Sheet（用户已明确不需要）
```

**文件命名要求**：`数据周报_YYYYMMDD_第N周.xlsx`

**实际实现问题**：

```
1. ✅ 概览 Sheet
2. ✅ 本周完成
3. ⚠️ 自采数据（主任务仅加粗，无背景色）
4. ✅ 后续计划
5. ✅ 会议待办
❌ 文件命名：`weekly-report-{uuid}.xlsx` - 不符合要求
```

**关键文件**：

- `backend/src/modules/export/export.service.ts:68-82` - Sheet 生成逻辑
- `backend/src/modules/export/export.service.ts:196-226` - SELF Sheet 样式（缺少背景色）
- `backend/src/modules/export/export.controller.ts:43` - 文件名设置
- `frontend/src/hooks/useExport.ts:22` - 前端下载文件名

**修复项**：

1. ~~添加"维度说明" Sheet（静态内容）~~ ❌ **已取消（用户明确不需要）**
2. ✅ SELF Sheet 主任务添加深色背景（`fill.fgColor`）
3. ✅ 修改文件名生成逻辑（需获取 `report.weekNumber` 和 `report.createdAt`）

**修复优先级**：⭐⭐⭐ 高（用户每周交付物，直接影响体验）

---

### 4. 版本选择器显示不完整（中优先级）

#### 问题描述

**需求规格说明书** (Section 4.1 全局导航与版本管理)：
> 列表项格式：`时间范围 (第N周) - 生成时间`

**实际实现** (`frontend/src/components/business/VersionSelector.tsx:91`)：

```tsx
<Option key={report.id} value={report.id}>
   {report.weekRange} {/* 仅显示时间范围，缺少周数和生成时间 */}
</Option>
```

**用户影响**：

- 🟡 **版本识别困难**：同一周内生成多个版本时无法区分
- 🟡 **缺少时间上下文**：用户不知道版本的创建时间

**修复方案**：

```tsx
<Option key={report.id} value={report.id}>
   {report.weekRange} (第{report.weekNumber}周) - {formatDate(report.createdAt)}
</Option>
```

**修复优先级**：⭐⭐ 中（影响用户体验但不阻塞核心流程）

---

### 5. 报表条目表格列定义不完整（中优先级）

#### 问题描述

**需求规格说明书** (Section 4.3 Tab 1 & Tab 3 列定义)：

```
维度、相关Jira、任务名称、状态/进度、生产环境状态、验证环境状态、复盘环境状态、预计工期、完成时间、负责人、备注
```

**实际实现** (`frontend/src/components/business/ReportTable.tsx:239`)：

```tsx
// 缺少列：维度、完成时间、备注
columns = [
   {title: 'Jira号', dataIndex: 'jiraKey'},
   {title: '任务名称', dataIndex: 'title'},
   {title: '状态', dataIndex: 'status'},
   {title: '负责人', dataIndex: 'assignee'},
   // ... 环境状态列
]
```

**修复优先级**：⭐⭐ 中（功能性缺失，但可能与业务方协商调整需求）

---

### 6. 事务处理性能风险（中优先级）

#### 问题描述

**技术债务**：`GenerateService.generateReport()` 在单个数据库事务内执行外部 API 调用（Jira 和 PostgreSQL），导致：

1. **SQLite 锁定时间过长**：外部请求耗时（可能数秒）会阻塞其他写操作
2. **事务超时风险**：Jira API 抖动可能导致事务失败

**关键文件**：

- `backend/src/modules/generate/generate.service.ts:47-70`

**当前代码**：

```typescript
return await this.dataSource.transaction(async (manager) => {
   // ❌ 外部 API 调用在事务内
   const [jiraDoneTasks, jiraPlanTasks, brvMetrics, revMetrics] = await Promise.all([
      this.jiraAdapter.fetchDoneTasks(),      // 外部 HTTP 请求
      this.jiraAdapter.fetchPlanTasks(),      // 外部 HTTP 请求
      this.sqlAdapter.fetchBrvMetrics(),      // 外部数据库查询
      this.sqlAdapter.fetchRevMetrics(),      // 外部数据库查询
   ]);
   // ... 数据写入
});
```

**推荐方案**：

```typescript
// ✅ 外部调用移到事务外
const [jiraDoneTasks, ...] = await Promise.all([...]);

return await this.dataSource.transaction(async (manager) => {
   // 仅数据库写入在事务内（耗时 < 100ms）
   await manager.save(report);
   await manager.save(metrics);
   // ...
});
```

**修复优先级**：⭐⭐ 中（当前用户量低风险可控，但需为扩展预留优化空间）

---

### 7. 外部数据库配置可能失效（中优先级）

#### 问题描述

**配置问题**：`backend/config/app.yaml:27` 中 `externalDatabases` 可能被注释掉，但 `config.schema.ts:63` 要求其必须存在。

**影响**：

- 🔴 应用可能无法启动（Schema 验证失败）
- 🔴 SQL Adapter 无连接池可用

**修复优先级**：⭐⭐ 中（需验证实际配置状态）

---

### 8. Jira 查询分页缺失（低优先级）

#### 问题描述

**需求规格说明书** 未明确要求分页，但 `JiraAdapter.executeJqlQuery()` 硬编码 `maxResults: 1000`，当周任务超过 1000
条时会静默截断。

**关键文件**：

- `backend/src/modules/generate/adapters/jira.adapter.ts:77`

**修复优先级**：⭐ 低（当前业务量小，但需文档化此限制）

---

### 9. 页面布局结构不合理（高优先级 - 用户新增）

#### 问题描述

**用户反馈**：

1. **版本选择器和生成按钮绑定数据**：当前版本选择器和操作按钮在 Card 内，与数据展示区混在一起
2. **新增按钮位置不合理**：表格的"新增一行"按钮应该放在表格底部，而不是顶部工具栏

**当前实现** (`frontend/src/features/report/ReportPage.tsx`)：

```tsx
<Card style={{marginBottom: 24}}>
   <Space>
      <VersionSelector currentReportId={report.id}/> {/* 绑定在 Card 内 */}
      <ActionBar reportId={report.id}/> {/* 绑定在 Card 内 */}
   </Space>
</Card>
<MetricDashboard/>
<TabEditor/>
```

**期望布局**：

```tsx
{/* 独立的全局操作区（最顶部，不绑定数据） */
}
<div className="global-actions">
   <VersionSelector/>
   <Button>生成本周周报</Button>
   <Button>导出 Excel</Button>
   <Button>会议待办</Button>
</div>

{/* 数据展示区 */
}
<MetricDashboard/>
<TabEditor>
   {/* 表格内容 */}
   <Table/>
   {/* 新增按钮放在表格底部 */}
   <Button>+ 新增一行</Button>
</TabEditor>
```

**关键文件**：

- `frontend/src/features/report/ReportPage.tsx` - 页面布局结构
- `frontend/src/components/business/ActionBar.tsx` - 操作栏组件
- `frontend/src/components/business/ReportTable.tsx` - 表格组件（新增按钮位置）

**修复优先级**：⭐⭐⭐ 高（用户明确要求，影响操作体验）

---

### 10. UI/样式整体优化需求（高优先级 - 用户新增）

#### 问题描述

**用户反馈**：现在的样式太丑了，需要全面优化。

**优化范围**：

1. **全局样式**：
   - 统一配色方案（主色、辅助色、背景色）
   - 优化间距、圆角、阴影等视觉细节
   - 统一字体大小和行高

2. **指标卡片**：
   - 优化卡片视觉层次（阴影、边框、渐变）
   - 优化进度条样式（更现代的视觉效果）
   - 优化图标和文字排版

3. **表格样式**：
   - 优化表格行列间距
   - 优化表头样式（背景色、字体粗细）
   - 优化斑马纹或行悬停效果
   - 优化操作按钮样式

4. **按钮与操作区**：
   - 统一按钮风格（大小、圆角、颜色）
   - 优化按钮组布局（间距、对齐）
   - 添加按钮悬停和点击效果

5. **侧边栏**：
   - 优化 Drawer 动画效果
   - 优化文本域样式

**关键文件**：

- `frontend/src/features/report/ReportPage.tsx` - 页面主容器
- `frontend/src/features/report/MetricDashboard.tsx` - 指标卡片
- `frontend/src/components/business/StackedProgress.tsx` - 进度条
- `frontend/src/components/business/ReportTable.tsx` - 表格
- `frontend/src/components/business/TreeTable.tsx` - 树形表格
- `frontend/src/components/business/ActionBar.tsx` - 操作栏
- `frontend/src/features/sidebar/MeetingSidebar.tsx` - 侧边栏
- 新增：`frontend/src/styles/theme.ts` - 主题配置（可选）

**修复优先级**：⭐⭐⭐ 高（用户明确要求，直接影响用户体验和产品印象）

---

### 11-13. 其他细节差距

| ID | 问题                                       | 优先级 | 文件                                                     |
|----|------------------------------------------|-----|--------------------------------------------------------|
| 11 | 删除按钮仅前端占位，后端未实现                          | ⭐ 低 | `frontend/src/components/business/ReportTable.tsx:127` |
| 12 | `contentJson` 在不同 API 返回格式不一致（字符串 vs 对象） | ⭐ 低 | `backend/src/modules/generate/generate.service.ts:216` |
| 13 | 错误处理缺少结构化错误码，全部返回 500                    | ⭐ 低 | 多个 Controller                                          |

---

## 🎯 实施计划

### 阶段 1：高优先级修复（预计 2-3 天）

#### Task 1.1：API 路径标准化（兼容层方案）

**执行者**：Codex 后端专家
**预期产物**：

1. 在 `ReportsController` 添加路由别名：
   ```typescript
   @Post('generate')  // 新增：符合规格的路径
   @Post()            // 保留：兼容现有前端
   async generateReport() { ... }
   ```
2. 前端逐步迁移到新路径（可分阶段完成）

**关键文件**：

- `backend/src/modules/reports/reports.controller.ts`
- `backend/src/modules/generate/generate.controller.ts`
- `backend/src/modules/export/export.controller.ts`

---

#### Task 1.2：SQL 查询适配器修复

**执行者**：Codex 后端专家
**预期产物**：

1. 修改 `executeQuery` 方法签名支持参数：
   ```typescript
   private async executeQuery(dbName: string, sql: string, params?: any[])
   ```
2. 修复 `normalizeMetrics` 字段映射逻辑
3. 验证 `app.yaml` 中 `externalDatabases` 配置有效

**关键文件**：

- `backend/src/modules/generate/adapters/sql.adapter.ts`
- `backend/config/app.yaml`

---

#### Task 1.3：Excel 导出规格对齐

**执行者**：Codex 后端专家
**预期产物**：

1. ~~添加"维度说明" Sheet（静态内容）~~ ❌ **已取消（用户明确不需要）**
2. ✅ SELF Sheet 主任务行添加背景色：
   ```typescript
   rootRow.fill = {
     type: 'pattern',
     pattern: 'solid',
     fgColor: { argb: 'FFD3D3D3' }  // 深灰色背景
   };
   ```
3. ✅ 修改文件名生成逻辑：
   ```typescript
   // 在 ExportController 中先查询 Report
   const report = await this.reportService.findById(reportId);
   const filename = `数据周报_${format(report.createdAt, 'yyyyMMdd')}_第${report.weekNumber}周.xlsx`;
   ```

**关键文件**：

- `backend/src/modules/export/export.service.ts`
- `backend/src/modules/export/export.controller.ts`
- `frontend/src/hooks/useExport.ts`

---

#### Task 1.4：页面布局结构重构（用户新增）

**执行者**：Gemini 前端专家
**预期产物**：

1. **全局操作区独立化**：
   ```tsx
   // 在 ReportPage 最顶部添加独立的操作栏
   <div className="page-header" style={{
     padding: '16px 24px',
     background: '#fff',
     marginBottom: '16px',
     boxShadow: '0 1px 2px rgba(0,0,0,0.06)'
   }}>
     <Space size="large">
       {/* 版本选择器 - 不依赖当前数据 */}
       <VersionSelector />

       {/* 操作按钮组 - 不依赖当前数据 */}
       <Space>
         <Button type="primary" icon={<PlusOutlined />}>生成本周周报</Button>
         <Button icon={<DownloadOutlined />}>导出 Excel</Button>
         <Button icon={<MenuOutlined />}>会议待办</Button>
       </Space>
     </Space>
   </div>

   {/* 数据展示区 - 独立渲染 */}
   <div className="page-content" style={{ padding: '0 24px' }}>
     <MetricDashboard />
     <TabEditor />
   </div>
   ```

2. **表格新增按钮位置调整**：
   ```tsx
   // 在 ReportTable 组件中
   <div>
     <Table dataSource={data} columns={columns} />

     {/* 新增按钮放在表格底部 */}
     <div style={{ marginTop: '16px', textAlign: 'left' }}>
       <Button type="dashed" icon={<PlusOutlined />} onClick={handleAdd}>
         新增一行
       </Button>
     </div>
   </div>
   ```

**关键文件**：

- `frontend/src/features/report/ReportPage.tsx` - 重构布局结构
- `frontend/src/components/business/ActionBar.tsx` - 拆分操作按钮逻辑
- `frontend/src/components/business/ReportTable.tsx` - 调整新增按钮位置
- `frontend/src/components/business/TreeTable.tsx` - 调整新增按钮位置

**修复优先级**：⭐⭐⭐ 高（用户明确要求）

---

#### Task 1.5：UI/样式全面优化（用户新增）

**执行者**：Gemini 前端专家
**预期产物**：

1. **全局主题配置**（可选，建议创建）：
   ```typescript
   // frontend/src/styles/theme.ts
   export const theme = {
     colors: {
       primary: '#1677ff',      // 主色
       success: '#52c41a',      // 成功色
       warning: '#faad14',      // 警告色
       error: '#ff4d4f',        // 错误色
       background: '#f5f5f5',   // 背景色
       cardBg: '#ffffff',       // 卡片背景
     },
     spacing: {
       xs: '8px',
       sm: '12px',
       md: '16px',
       lg: '24px',
       xl: '32px',
     },
     borderRadius: {
       sm: '4px',
       md: '8px',
       lg: '12px',
     },
     shadows: {
       card: '0 2px 8px rgba(0,0,0,0.08)',
       hover: '0 4px 12px rgba(0,0,0,0.12)',
     }
   };
   ```

2. **指标卡片优化**：
   - 添加精致的阴影和圆角
   - 优化卡片内边距和元素间距
   - 优化进度条视觉效果（渐变色、动画）

3. **表格样式优化**：
   - 优化表头样式（字体粗细、背景色、边框）
   - 优化行间距和列间距
   - 添加行悬停效果
   - 优化操作按钮样式（大小、颜色、间距）

4. **按钮统一优化**：
   - 统一按钮圆角、高度
   - 优化按钮颜色和悬停效果
   - 优化按钮组间距

5. **页面整体优化**：
   - 优化页面背景色
   - 优化卡片间距
   - 优化文字大小和行高

**关键文件**：

- 新增：`frontend/src/styles/theme.ts` - 主题配置
- `frontend/src/features/report/ReportPage.tsx` - 页面容器样式
- `frontend/src/features/report/MetricDashboard.tsx` - 指标卡片样式
- `frontend/src/components/business/StackedProgress.tsx` - 进度条样式
- `frontend/src/components/business/MetricCard.tsx` - 卡片组件样式
- `frontend/src/components/business/ReportTable.tsx` - 表格样式
- `frontend/src/components/business/TreeTable.tsx` - 树形表格样式
- `frontend/src/features/sidebar/MeetingSidebar.tsx` - 侧边栏样式

**修复优先级**：⭐⭐⭐ 高（用户明确要求，提升整体产品质量）

---

### 阶段 2：中优先级优化（预计 1-2 天）

#### Task 2.1：版本选择器格式优化

**执行者**：Gemini 前端专家
**预期产物**：

```tsx
<Option key={report.id} value={report.id}>
   {report.weekRange} (第{report.weekNumber}周) - {dayjs(report.createdAt).format('YYYY-MM-DD HH:mm')}
</Option>
```

**注意**：此任务已被 Task 1.4（页面布局结构重构）包含，版本选择器会在布局重构时一并优化。

**关键文件**：

- `frontend/src/components/business/VersionSelector.tsx`

---

#### Task 2.2：报表列定义补全（可选，需业务方确认）

**执行者**：Gemini 前端专家
**预期产物**：

- 添加"维度"列（枚举值：数据开发、数据治理等）
- 添加"完成时间"列（日期选择器）
- 添加"备注"列（文本输入）

**关键文件**：

- `frontend/src/components/business/ReportTable.tsx`
- `backend/src/modules/generate/adapters/jira.adapter.ts`（需从 Jira 获取对应字段）

---

#### Task 2.3：事务边界优化

**执行者**：Codex 后端专家
**预期产物**：

- 将外部 API 调用移到事务外
- 仅数据库写入在事务内

**关键文件**：

- `backend/src/modules/generate/generate.service.ts`

---

### 阶段 3：低优先级完善（预计 1 天）

#### Task 3.1：Jira 分页支持

**执行者**：Codex 后端专家
**预期产物**：

- 实现 `startAt` + `maxResults` 循环查询
- 文档化单周最大任务数限制（建议 5000 条）

---

#### Task 3.2：前端删除功能实现

**执行者**：Gemini 前端专家 + Codex 后端专家
**预期产物**：

- 后端添加 `DELETE /api/items/:id` 端点
- 前端连接删除按钮事件

---

## 📈 风险与缓解

| 风险                    | 缓解措施                                     |
|-----------------------|------------------------------------------|
| **API 路径变更导致前端破坏**    | 使用兼容层（新旧路径共存），前端分阶段迁移                    |
| **SQL 查询参数化可能影响现有逻辑** | 先在测试环境验证，确保查询结果一致                        |
| **Excel 文件名生成需额外查询**  | 可在 ExportController 中复用现有 ReportsService |
| **事务优化可能引入数据不一致风险**   | 保持原子性，仅将外部调用移出事务，内部逻辑不变                  |
| **页面布局重构可能影响现有功能**    | 保持原有逻辑不变，仅调整 DOM 结构和样式；重构后完整测试所有功能       |
| **样式优化可能影响响应式布局**     | 使用 Ant Design 的栅格系统，确保移动端适配；测试不同屏幕尺寸     |

---

## 📚 参考文档

- **需求规格说明书**: `数据周报自动化系统 - 需求规格说明书.md` (V3.0)
- **项目上下文文档**: `CLAUDE.md`
- **Codex 分析报告**: Session `019beba8-c5a1-7750-bb40-62782301e0fc`
- **Gemini 分析报告**: Session `9b0e273f-0624-4853-b507-816afbd6b1c8`

---

## 🔄 后续建议

1. **测试覆盖**：在修复过程中为关键模块添加单元测试（IdService、SqlAdapter、ExportService）
2. **文档同步**：每次修改后同步更新需求规格说明书和 CLAUDE.md
3. **版本管理**：建议使用语义化版本号（当前 V3.0）标记每次修复的里程碑
4. **性能监控**：生产环境部署后监控周报生成耗时，验证事务优化效果

---

**报告生成者**: Claude Code (猫娘浮浮酱 喵～)
**最后更新**: 2026-01-24
**最后修改**: 2026-01-24（根据用户反馈调整：取消维度说明 Sheet，新增 UI 优化和布局重构需求）
