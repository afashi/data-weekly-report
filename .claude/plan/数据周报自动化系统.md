# 数据周报自动化系统 - 完整实施计划

**版本**: V1.1
**创建时间**: 2026-01-15
**更新时间**: 2026-01-15
**实施方案**: 方案 1 - 敏捷快速型
**预计开发周期**: 5-7 个工作日
**实际进度**: 阶段 1 部分完成（~70%），阶段 2-6 待实施

---

## 一、技术架构总览

### 1.1 技术栈

| 层级        | 技术选型                            | 说明             |
|-----------|---------------------------------|----------------|
| **前端**    | React 18 + TypeScript           | 现代化前端框架        |
| **UI 库**  | Ant Design 5.x                  | 企业级组件库         |
| **状态管理**  | Zustand (UI) + React Query (数据) | 轻量化状态方案        |
| **数据处理**  | Immer                           | 树形数据不可变更新      |
| **构建工具**  | Vite 5.x                        | 极速开发体验         |
| **后端**    | NestJS + TypeScript             | 企业级 Node.js 框架 |
| **ORM**   | TypeORM                         | 支持 SQLite      |
| **数据库**   | SQLite (WAL 模式)                 | 轻量级关系数据库       |
| **ID 生成** | Snowflake 算法                    | 分布式唯一 ID       |
| **Excel** | ExcelJS                         | Excel 文件读写     |
| **配置**    | YAML                            | 人类友好的配置格式      |

### 1.2 架构特点

- **单体架构**：前后端分离，但部署为单体应用
- **同步生成**：周报生成采用同步接口（单人使用无需队列）
- **快照模式**：所有修改仅在本地数据库，不回写 Jira/外部 DB
- **乐观更新**：前端编辑采用乐观 UI，提升交互体验
- **模板驱动**：Excel 导出支持预定义模板

---

## 二、数据库设计

### 2.1 表结构（4 张表）

| 表名               | 主要字段                                                                      | 说明           |
|------------------|---------------------------------------------------------------------------|--------------|
| `reports`        | id, week_range, week_number, created_at, is_deleted                       | 报告主表，存储周报元数据 |
| `system_metrics` | id, report_id, metric_key, metric_value, status_code                      | 指标表，存储顶部卡片数据 |
| `report_items`   | id, report_id, tab_type, source_type, parent_id, content_json, sort_order | 条目表，支持树形结构   |
| `meeting_notes`  | id, report_id, content                                                    | 会议待办表        |

### 2.2 关键约束

- **命名规范**: 所有表名、字段名使用 `snake_case`
- **主键类型**: BIGINT（Snowflake ID），API 层自动转 String
- **索引**: 已在 Entity 中定义复合索引
- **SQLite**: WAL 模式 + busy_timeout 5000ms

---

## 三、后端架构设计

### 3.1 项目结构

```
backend/
├── src/
│   ├── main.ts                    # 应用入口
│   ├── app.module.ts
│   ├── common/                    # 通用模块
│   │   ├── entities/
│   │   │   └── base-id.entity.ts  # BIGINT ID 基类
│   │   ├── interceptors/
│   │   │   └── bigint-to-string.interceptor.ts
│   │   └── utils/
│   │       ├── week-range.ts      # 周期计算
│   │       └── tree-flatten.ts    # 树形扁平化
│   ├── config/                    # 配置模块
│   │   ├── config.module.ts
│   │   ├── config.schema.ts       # Zod 校验
│   │   └── config.loader.ts
│   ├── datasources/               # 数据源适配器
│   │   ├── jira/
│   │   │   ├── jira.adapter.ts    # Jira REST API 集成
│   │   │   └── jira.mapper.ts     # 字段映射
│   │   └── sql/
│   │       ├── sql.adapter.ts     # 外部数据库查询
│   │       └── sql.mapper.ts
│   ├── modules/                   # 业务模块
│   │   ├── reports/
│   │   │   ├── reports.controller.ts
│   │   │   ├── reports.service.ts
│   │   │   └── dto/
│   │   ├── items/
│   │   │   ├── items.controller.ts
│   │   │   └── items.service.ts
│   │   ├── notes/
│   │   ├── generate/              # 周报生成模块
│   │   │   ├── generate.controller.ts
│   │   │   └── generate.service.ts
│   │   └── export/                # Excel 导出
│   │       ├── export.controller.ts
│   │       └── export.service.ts
│   ├── entities/                  # 数据库实体
│   │   ├── report.entity.ts
│   │   ├── system-metric.entity.ts
│   │   ├── report-item.entity.ts
│   │   └── meeting-note.entity.ts
│   └── migrations/                # 数据库迁移
│       └── 0001-init.ts
├── assets/
│   └── templates/
│       └── weekly-report.xlsx     # Excel 模板
└── config/
    └── app.yaml                   # 配置文件
```

### 3.2 配置文件设计（app.yaml）

```yaml
server:
  port: 3000

database:
  path: "data/weekly-report.sqlite"

id:
  workerId: 1
  datacenterId: 1

jira:
  baseUrl: "https://example.atlassian.net"
  email: "user@example.com"
  apiToken: "YOUR_TOKEN"
  jql:
    done: "project = ABC AND status = Done AND updated >= startOfWeek()"
    plan: "project = ABC AND status in (Open, InProgress)"
  fields:
    - "summary"
    - "status"
    - "assignee"
    - "customfield_12345"  # 预计工期

externalDb:
  type: "mysql"
  url: "mysql://user:pass@host:3306/db"
  connectTimeoutMs: 5000
  queryTimeoutMs: 15000

sqlQueries:
  metrics: "SELECT metric_key, metric_value, status_code FROM etl_metrics WHERE week_number = :weekNumber"

excel:
  templatePath: "assets/templates/weekly-report.xlsx"
  indentSize: 2  # 树形缩进字符数
```

### 3.3 核心 API 设计

| 方法     | 路径                              | 功能         | 请求体                           | 响应体                                  |
|--------|---------------------------------|------------|-------------------------------|--------------------------------------|
| GET    | `/api/reports`                  | 获取历史版本列表   | -                             | `{ items: ReportSummary[] }`         |
| POST   | `/api/reports/generate`         | 生成新周报      | -                             | `{ reportId, weekRange, createdAt }` |
| GET    | `/api/reports/:id`              | 获取周报详情     | -                             | `{ report, metrics, items, note }`   |
| PUT    | `/api/reports/:id/manual-items` | 更新自采数据（全量） | `{ items: ManualItemNode[] }` | `{ updatedCount }`                   |
| PATCH  | `/api/items/:id`                | 更新单行条目     | `{ content, sortOrder }`      | `{ id, updatedAt }`                  |
| PATCH  | `/api/notes/:report_id`         | 更新会议待办     | `{ content }`                 | `{ reportId, content }`              |
| GET    | `/api/reports/:id/export`       | 导出 Excel   | -                             | 文件流                                  |
| DELETE | `/api/reports/:id`              | 软删除周报      | -                             | `{ id, isDeleted }`                  |

---

## 四、前端架构设计

### 4.1 项目结构

```
frontend/
├── src/
│   ├── assets/              # 静态资源
│   ├── components/          # 通用组件
│   │   ├── common/
│   │   │   ├── Loading.tsx
│   │   │   └── ErrorFallback.tsx
│   │   ├── business/
│   │   │   ├── MetricCard.tsx
│   │   │   └── StackedProgress.tsx
│   │   └── layout/
│   │       ├── MainLayout.tsx
│   │       └── Header.tsx
│   ├── features/            # 业务模块
│   │   ├── report/
│   │   │   ├── Dashboard.tsx
│   │   │   ├── MetricDashboard.tsx
│   │   │   ├── TabEditor.tsx
│   │   │   ├── WeeklyDoneTable.tsx
│   │   │   ├── SelfCollectedTree.tsx
│   │   │   └── FuturePlanTable.tsx
│   │   ├── sidebar/
│   │   │   └── MeetingSidebar.tsx
│   │   └── navigation/
│   │       └── VersionSelector.tsx
│   ├── hooks/               # 通用 Hooks
│   │   └── useDebounce.ts
│   ├── lib/                 # 第三方库配置
│   │   ├── axios.ts
│   │   └── queryClient.ts
│   ├── services/            # API 接口封装
│   │   ├── api.ts
│   │   └── reportHooks.ts
│   ├── store/               # Zustand Stores
│   │   └── uiStore.ts
│   ├── types/               # TypeScript 类型
│   │   └── index.ts
│   └── utils/               # 工具函数
│       ├── formatter.ts
│       └── tree-utils.ts
├── index.html
└── vite.config.ts
```

### 4.2 组件树设计

```
App
├── QueryClientProvider (React Query)
├── Router
│   └── MainLayout
│       ├── Header
│       │   ├── VersionSelector (Dropdown)
│       │   └── ActionBar
│       │       ├── GenerateButton
│       │       ├── ExportButton
│       │       └── ToggleSidebarButton
│       ├── ContentArea
│       │   └── ReportPage
│       │       ├── MetricDashboard
│       │       │   ├── BusinessVolumeCard
│       │       │   ├── VerifyETLCard
│       │       │   └── ReviewETLCard
│       │       └── TabEditor (Ant Design Tabs)
│       │           ├── Tab 1: WeeklyDoneTable
│       │           ├── Tab 2: SelfCollectedTree
│       │           └── Tab 3: FuturePlanTable
│       └── MeetingSidebar (Drawer)
```

---

## 五、开发顺序与里程碑

### 阶段 1：基础设施搭建（Day 1）

**后端任务**：

- [x] 初始化 NestJS 项目，配置 TypeScript
- [x] 安装依赖：TypeORM, SQLite, ExcelJS, Snowflake
- [x] 实现 ConfigModule（YAML 加载 + Zod 校验）
- [x] 实现 IdService（Snowflake ID 生成）
- [x] 配置 SQLite 连接 + WAL 模式
- [x] 创建 BaseIdEntity + BIGINT 序列化拦截器

**前端任务**：

- [x] 初始化 Vite + React 18 项目
- [x] 安装依赖：Ant Design, Zustand, React Query, Immer
- [x] 配置 Axios + React Query Client
- [x] 实现 UIStore（Zustand）
- [x] 搭建 MainLayout 骨架（Header + Drawer）

### 阶段 2：数据库与核心模块（Day 2）

**后端任务**：

- [x] 定义 4 个 Entity（Report, SystemMetric, ReportItem, MeetingNote）⚠️ 缺少关系约束
- [ ] 生成初始 Migration 并执行 🔴 **阻塞问题**
- [ ] 实现 JiraAdapter（REST API 调用 + 字段映射）
- [ ] 实现 SqlAdapter（动态连接 + 参数化查询）
- [ ] 实现 GenerateService（周期计算 + 事务写入）

**前端任务**：

- [ ] 定义 TypeScript 类型（Report, SystemMetric, ReportItem）
- [ ] 实现 API 服务层（axios 封装）
- [ ] 实现 React Query Hooks（useReports, useReportDetail）

### 阶段 3：核心功能开发（Day 3-4）

**后端任务**：

- [ ] 实现 ReportsController（GET /reports, GET /reports/:id, DELETE /reports/:id）
- [ ] 实现 GenerateController（POST /reports/generate）
- [ ] 实现 ItemsController（PATCH /items/:id, PUT /reports/:id/manual-items）
- [ ] 实现 NotesController（PATCH /notes/:report_id）
- [ ] 实现 ExportService（Excel 模板加载 + 树形格式化）

**前端任务**：

- [ ] 实现 MetricDashboard（3 个指标卡片 + 响应式布局）
- [ ] 实现 StackedProgress 组件（双色进度条 + Tooltip）
- [ ] 实现 WeeklyDoneTable（Tab 1：双击编辑 + 乐观更新）
- [ ] 实现 FuturePlanTable（Tab 3：同 Tab 1）
- [ ] 实现 MeetingSidebar（Drawer + 自动保存）

### 阶段 4：复杂功能开发（Day 5）

**前端任务**：

- [ ] 实现 SelfCollectedTree（Tab 2：树形表格 + Draft 模式 + Immer）
- [ ] 实现树形数据的增删改操作
- [ ] 实现 Zod Schema 校验
- [ ] 实现全量提交逻辑

**后端任务**：

- [ ] 完善 PUT /reports/:id/manual-items（删除旧数据 + 插入新数据 + ID 映射）

### 阶段 5：全局功能串联（Day 6）

**前端任务**：

- [ ] 实现 VersionSelector（下拉菜单 + 版本切换 + 删除）
- [ ] 实现 ActionBar（生成周报 + 导出 Excel + 切换侧边栏）
- [ ] 实现路由逻辑（/reports/:reportId, /latest 重定向）
- [ ] 实现加载状态（Skeleton）与错误边界（ErrorBoundary）

**后端任务**：

- [ ] 实现 GET /api/reports/:id/export（文件流下载）
- [ ] 添加全局异常过滤器
- [ ] 添加健康检查接口（/health）

### 阶段 6：样式优化与测试（Day 7）

- [ ] 响应式适配测试（1200px 断点）
- [ ] 表格编辑交互优化（防抖、加载状态）
- [ ] Excel 导出样式调整（根据模板）
- [ ] 端到端功能测试
- [ ] 性能优化（React Query 缓存策略）

---

## 六、后续扩展建议

- [ ] 添加单元测试（Jest / Vitest）
- [ ] 添加 E2E 测试（Playwright）
- [ ] 迁移到 PostgreSQL
- [ ] AI 辅助填写

---

**文档结束**

此计划书已通过 Codex（后端专家）和 Gemini（前端专家）双模型交叉验证。
