# Frontend 模块 - AI 上下文文档

> **最后更新**: 2026-01-23
> **模块状态**: 🟡 开发中（约 40% 完成）
> **技术栈**: React 18 + Ant Design 5 + Vite 7 + TypeScript 5

---

## 📋 模块概述

Frontend 模块是数据周报自动化系统的前端应用，负责周报编辑、可视化展示、Excel 导出等用户交互功能。

**核心职责**：

- 📊 **数据可视化**：指标看板、进度条、状态卡片
- ✏️ **周报编辑**：三个 Tab 页的数据编辑（DONE、SELF、PLAN）
- 🌲 **树形表格**：自采数据的主任务-子任务层级展示
- 📝 **会议待办**：侧边栏文本编辑器
- 🔄 **版本管理**：历史周报切换与删除
- 📥 **Excel 导出**：一键下载标准格式周报

---

## 🏗️ 模块结构

```
frontend/
├── src/
│   ├── main.tsx                         # 应用入口
│   ├── App.tsx                          # 根组件（路由配置）
│   ├── vite-env.d.ts                    # Vite 类型声明
│   ├── components/                      # UI 组件
│   │   ├── business/                    # 业务组件
│   │   │   ├── ActionBar.tsx            # 操作栏（生成、导出、待办）
│   │   │   ├── MetricCard.tsx           # 指标卡片
│   │   │   ├── StackedProgress.tsx      # 堆叠进度条
│   │   │   ├── ReportTable.tsx          # 报表表格（DONE、PLAN）
│   │   │   ├── TreeTable.tsx            # 树形表格（SELF）
│   │   │   ├── VersionSelector.tsx      # 版本选择器
│   │   │   └── index.ts                 # 导出索引
│   │   └── layout/
│   │       └── MainLayout.tsx           # 主布局
│   ├── features/                        # 功能模块
│   │   ├── report/                      # 周报页面
│   │   │   ├── ReportPage.tsx           # 周报详情页
│   │   │   ├── LatestReportResolver.tsx # 最新周报解析器
│   │   │   ├── MetricDashboard.tsx      # 指标看板
│   │   │   ├── TabEditor.tsx            # Tab 编辑器
│   │   │   └── index.ts
│   │   └── sidebar/                     # 侧边栏
│   │       ├── MeetingSidebar.tsx       # 会议待办侧边栏
│   │       ├── NotesSidebar.tsx         # 备注侧边栏
│   │       └── index.ts
│   ├── hooks/                           # React Hooks
│   │   ├── useGenerate.ts               # 周报生成 Hook
│   │   ├── useReports.ts                # 周报查询 Hook
│   │   ├── useItems.ts                  # 条目编辑 Hook
│   │   ├── useNotes.ts                  # 会议待办 Hook
│   │   ├── useExport.ts                 # Excel 导出 Hook
│   │   └── index.ts
│   ├── services/                        # API 服务
│   │   ├── http-client.ts               # HTTP 客户端封装
│   │   ├── generate-api.ts              # 周报生成 API
│   │   ├── report-api.ts                # 周报查询 API
│   │   ├── item-api.ts                  # 条目编辑 API
│   │   ├── notes-api.ts                 # 会议待办 API
│   │   ├── export-api.ts                # Excel 导出 API
│   │   └── index.ts
│   ├── store/                           # 状态管理
│   │   └── uiStore.ts                   # UI 状态（侧边栏、加载状态）
│   ├── types/                           # 类型定义
│   │   ├── report.ts                    # 周报相关类型
│   │   ├── api.ts                       # API 响应类型
│   │   └── index.ts
│   └── lib/                             # 工具库
│       ├── axios.ts                     # Axios 实例配置
│       └── queryClient.ts               # React Query 配置
├── public/                              # 静态资源
├── index.html                           # HTML 模板
├── package.json
├── tsconfig.json
├── tsconfig.node.json
└── vite.config.ts                       # Vite 配置
```

---

## 📦 核心模块详解

### 1. Report 功能模块

**路径**: `src/features/report/`

**职责**：

- 周报详情页展示
- 指标看板渲染
- Tab 编辑器管理
- 最新周报自动跳转

**关键组件**：

#### ReportPage.tsx

- **职责**：周报详情页主组件
- **数据源**：`useReportDetail(reportId)` Hook
- **子组件**：
    - `VersionSelector` - 版本选择器
    - `ActionBar` - 操作栏
    - `MetricDashboard` - 指标看板
    - `TabEditor` - Tab 编辑器
    - `MeetingSidebar` - 会议待办侧边栏

#### MetricDashboard.tsx

- **职责**：顶部指标看板（3 个卡片）
- **布局**：Grid 栅格布局（12:6:6）
- **卡片类型**：
    1. **业务量概览**：总数 + 双色堆叠进度条 + 分类数值
    2. **验证环境加载**：加载时间 + 状态指示（Loading/Success）
    3. **复盘环境加载**：加载时间 + 状态指示（Loading/Success）

#### TabEditor.tsx

- **职责**：三个 Tab 页的编辑器
- **Tab 类型**：
    - **DONE（本周完成）**：标准二维表格
    - **SELF（自采数据）**：树形表格（主任务-子任务）
    - **PLAN（后续计划）**：标准二维表格

#### LatestReportResolver.tsx

- **职责**：自动解析最新周报并跳转
- **逻辑**：查询历史周报列表 → 获取最新 ID → 重定向到 `/reports/:id`

### 2. Sidebar 功能模块

**路径**: `src/features/sidebar/`

**职责**：

- 会议待办侧边栏
- 备注侧边栏

**关键组件**：

#### MeetingSidebar.tsx

- **职责**：会议待办编辑器
- **UI**：Ant Design Drawer + TextArea
- **功能**：
    - 纯文本编辑
    - 自动保存或快捷键保存
    - 关联当前周报 ID

### 3. Business 组件库

**路径**: `src/components/business/`

**职责**：

- 业务相关的可复用组件

**关键组件**：

#### ActionBar.tsx

- **职责**：全局操作栏
- **按钮**：
    - **生成本周周报**：触发后端生成逻辑
    - **导出 Excel**：下载当前周报
    - **待办事项**：控制侧边栏展开/收起

#### VersionSelector.tsx

- **职责**：历史版本切换器
- **UI**：Ant Design Select 下拉菜单
- **功能**：
    - 展示所有历史周报版本
    - 列表项格式：`时间范围 (第N周) - 生成时间`
    - 支持删除特定历史版本（软删除）
    - 切换版本时无刷新重载

#### MetricCard.tsx

- **职责**：指标卡片组件
- **Props**：
    - `title` - 卡片标题
    - `value` - 主要数值
    - `status` - 状态（loading、success、normal）
    - `extra` - 额外内容（如进度条）

#### StackedProgress.tsx

- **职责**：双色堆叠进度条
- **Props**：
    - `leftValue` - 左段数值
    - `rightValue` - 右段数值
    - `leftLabel` - 左段标签
    - `rightLabel` - 右段标签
    - `leftColor` - 左段颜色
    - `rightColor` - 右段颜色
- **交互**：Hover 显示 Tooltip

#### ReportTable.tsx

- **职责**：标准二维表格（DONE、PLAN）
- **列定义**：
    - 维度
    - 相关 Jira
    - 任务名称
    - 状态/进度
    - 生产环境状态
    - 验证环境状态
    - 复盘环境状态
    - 预计工期
    - 完成时间
    - 负责人
    - 备注
    - 操作（编辑、删除）
- **功能**：
    - 单行编辑
    - 新增空行
    - 删除行

#### TreeTable.tsx

- **职责**：树形表格（SELF）
- **层级**：
    - **Level 1（主任务）**：任务概览（名称、Jira号、整体状态）
    - **Level 2（子任务）**：执行细节（步骤名、工期、负责人、完成情况）
- **功能**：
    - 新增主任务
    - 新增子任务
    - 展开/收起
    - 全量树结构提交

### 4. Hooks 层

**路径**: `src/hooks/`

**职责**：

- 封装业务逻辑与数据请求
- 基于 React Query 实现数据缓存与状态管理

**关键 Hooks**：

#### useGenerate.ts

- **职责**：周报生成逻辑
- **方法**：
    - `generateReport(dto)` - 触发生成
    - `isGenerating` - 生成中状态
    - `error` - 错误信息

#### useReports.ts

- **职责**：周报查询逻辑
- **方法**：
    - `useReportList()` - 获取历史周报列表
    - `useReportDetail(id)` - 获取指定周报详情
    - `deleteReport(id)` - 软删除周报

#### useItems.ts

- **职责**：条目编辑逻辑
- **方法**：
    - `updateItem(id, data)` - 更新单行条目
    - `updateManualItems(reportId, tree)` - 全量更新自采数据

#### useNotes.ts

- **职责**：会议待办逻辑
- **方法**：
    - `updateNotes(reportId, content)` - 更新会议待办

#### useExport.ts

- **职责**：Excel 导出逻辑
- **方法**：
    - `exportReport(reportId)` - 导出 Excel

### 5. Services 层

**路径**: `src/services/`

**职责**：

- 封装 HTTP 请求
- 统一错误处理
- 类型安全的 API 调用

**关键服务**：

#### http-client.ts

- **职责**：Axios 实例封装
- **功能**：
    - 请求拦截器（添加 Token）
    - 响应拦截器（统一错误处理）
    - 超时配置

#### generate-api.ts

- **职责**：周报生成 API
- **方法**：
    - `generateReport(dto)` - POST /api/generate
    - `checkHealth()` - GET /api/generate/health

#### report-api.ts

- **职责**：周报查询 API
- **方法**：
    - `getReportList()` - GET /api/reports
    - `getReportDetail(id)` - GET /api/reports/:id
    - `deleteReport(id)` - DELETE /api/reports/:id

#### item-api.ts

- **职责**：条目编辑 API
- **方法**：
    - `updateItem(id, data)` - PATCH /api/items/:id
    - `updateManualItems(reportId, tree)` - PUT /api/reports/:id/manual-items

#### notes-api.ts

- **职责**：会议待办 API
- **方法**：
    - `updateNotes(reportId, content)` - PATCH /api/notes/:report_id

#### export-api.ts

- **职责**：Excel 导出 API
- **方法**：
    - `exportReport(reportId)` - GET /api/reports/:id/export

### 6. Store 层

**路径**: `src/store/`

**职责**：

- 全局 UI 状态管理
- 基于 Zustand 实现

**关键 Store**：

#### uiStore.ts

- **职责**：UI 状态管理
- **状态**：
    - `isSidebarOpen` - 侧边栏是否打开
    - `isLoading` - 全局加载状态
- **方法**：
    - `openSidebar()` - 打开侧边栏
    - `closeSidebar()` - 关闭侧边栏
    - `setLoading(loading)` - 设置加载状态

---

## 🎨 UI 设计规范

### 布局规范

**指标看板布局**：

- Grid 栅格布局（24 栅格系统）
- 比例：12:6:6（业务量卡片 50%，两个环境卡片各 25%）

**表格布局**：

- 标准二维表格：Ant Design Table
- 树形表格：Ant Design Table（expandable）

**侧边栏布局**：

- Ant Design Drawer
- 宽度：400px

### 颜色规范

**进度条颜色**：

- 左段（加工单）：`#1890ff`（蓝色）
- 右段（补录单）：`#52c41a`（绿色）

**状态颜色**：

- Loading：`#faad14`（橙色）
- Success：`#52c41a`（绿色）
- Normal：`#d9d9d9`（灰色）

### 交互规范

**Hover 效果**：

- 进度条 Hover：显示 Tooltip
- 表格行 Hover：背景色变化

**点击效果**：

- 按钮点击：Loading 状态
- 版本切换：无刷新重载

---

## 🔧 开发指南

### 添加新的页面

```bash
# 在 features/ 创建功能模块
mkdir src/features/<模块名>
touch src/features/<模块名>/index.tsx

# 在 App.tsx 添加路由
<Route path="/<路径>" element={<YourComponent />} />
```

### 添加新的组件

```bash
# 在 components/business/ 创建业务组件
touch src/components/business/YourComponent.tsx

# 在 index.ts 导出
export { YourComponent } from './YourComponent';
```

### 添加新的 Hook

```bash
# 在 hooks/ 创建 Hook
touch src/hooks/useYourHook.ts

# 在 index.ts 导出
export { useYourHook } from './useYourHook';
```

### 调试技巧

**React Query DevTools**：

- 开发模式自动显示
- 查看缓存数据与请求状态

**Zustand DevTools**：

- 安装浏览器扩展
- 查看全局状态变化

**Network 面板**：

- 查看 API 请求与响应
- 检查请求参数与响应数据

---

## 🚨 关键约束

1. **类型安全**：禁止使用 any，必须明确类型定义
2. **组件拆分**：单个组件不超过 300 行
3. **Hook 封装**：业务逻辑必须封装在 Hook 中
4. **API 调用**：必须通过 Services 层调用
5. **状态管理**：UI 状态使用 Zustand，数据状态使用 React Query

---

## ⚠️ 已知问题

1. **部分组件未完成**：TreeTable、StackedProgress 功能不完整
2. **Excel 导出未实现**：前端下载逻辑待开发
3. **错误处理不完善**：缺少全局错误边界
4. **性能优化**：大表格需考虑虚拟滚动

---

## 📚 参考资料

- [React 官方文档](https://react.dev/)
- [Ant Design 组件库](https://ant.design/)
- [React Query 文档](https://tanstack.com/query/latest)
- [Zustand 文档](https://zustand-demo.pmnd.rs/)

---

**文档生成时间**: 2026-01-23
**文档版本**: V2.0
**维护者**: AI Assistant
